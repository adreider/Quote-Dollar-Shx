import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    minHeight: 200,
    width: '100%',
    backgroundColor: '#bd93f9',
    padding: 30,
  },

  title: {
    fontSize: 35,
    color: "#fff",
    fontWeight: "bold"
  }
});
